package io.autoinvestor.configuration;

// @Configuration
public class SecurityConfiguration {

    // @Bean
    // public SecurityWebFilterChain securityWebFilterChain(ServerHttpSecurity http)
    // {
    //
    // http.csrf(ServerHttpSecurity.CsrfSpec::disable).authorizeExchange().anyExchange().permitAll();
    // return http.build();
    // }
    /*
     * @Bean public ReactiveJwtDecoder reactiveJwtDecoder() { return
     * NimbusReactiveJwtDecoder
     * .withJwkSetUri("https://dev-90706258.okta.com/oauth2/v1/keys") .build(); }
     */

}
